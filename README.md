# Excel
This project is getting to know about working of MsExcel
A cookie company management offsite executives want to look at the numbers of the products selling for strategic implimentations.
First we will add data into a spreadsheet to analyze.
Summary:-
Short cut to open "Format" dialuge box :- Crtl + 1
under summary tab, management offsite wants to know the revenue of each quarter. 
- I learnt on how to format a cell individually or locking the format tool to format multiple cells, i even looked at how to insert a dollar sign in the cell along with a number,       remove or add decimal points.
- i have also learnt, how date and time works and how we can format them accordingly to the given data. if the excel doesnt recognize the date, it stays the same aligned to the left    of the cell it doesnt shift to the right. In date if we dont write the year, it converts the date into "day-year" format, when we type the year also it stays the same. and for the    time, excel doesnt recognize AM and PM, when we write doen the time we need to mention PM or write the time in 24-hour format otherwise it will display AM time.
- Number of cookie, suppose we want to only insert values in particular cells, all we have to do is select all the cells we want to insert and start typing even of press tab/enter      the crusor stays in the selected cells.
- if you want to insert a table for the data use (CRLT + T) or the table or simply select all the cells which you want to include in the table and hover over to tables and do the changes as needed according to the use case. and if you want to show graphs and charts in the table you can use conditional formatting.

Income:-
coming to the income statement we have to calculate a number of things, excel provides this option with the help of formulas
- at the bottom of the page, we will see the basic arthemtic formulas that the excel spreadsheet generates for the rows/columns you have selected. so, formulas are the one way to       calculate the table without solely reliying on the status bar.
- in excel if you want to type a formula start with "=" operator, and lets suppose if you change any value in the given cell it does not aautomatically update the value in the formula instead what you can do is in the formula give refernce to cell for example cell "B2", so if you cahnge the value in the cell B2 it will automatically update the value in the result cell. 
- "CRLT + Enter" makes it easy to fill in all the different rows in the sheet if they are calculated with the same formula.
- if we want to lock in the formula or make it an obselete reference to all the cells we can use "$" infront of column name and row number(= $H$5)
- we can also calculate the value if they are on a different sheet in the same workbook. to lock in the value just use "$" before row value and column value.
- 
